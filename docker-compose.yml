version: '3.8'

services:
  backend:
    build:
      context: ./backend
    volumes:
      - ./backend:/usr/src/app
    ports:
      - '3000:3000'
    depends_on:
      - db

  db:
    image: mongo:latest
    ports:
      - '27017:27017'
